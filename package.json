{
  "type": "module",
  "scripts": {
    "postinstall": "pnpm --filter ./scripts install && pnpm run build:cli",
    "build": "node ./scripts/dist/build.js",
    "build:cli": "pnpm --filter=./scripts run build"
  },
  "devDependencies": {
    "@types/node": "^22.5.5",
    "typescript": "^5.6.2"
  }
}
